export const darkMode = {
    bgColor : '#000',
    textColor : "#fff",
    borderColor : "#fff"
}

export const lightMode = {
    bgColor: '#fff',
    textColor : "#333",
    borderColor : "#333",
}

export const theme = {
    darkMode,
    lightMode
}